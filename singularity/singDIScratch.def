# build environment for deepInterpolation data processing

Bootstrap: docker
From: ubuntu:20.04

%environment
    export TMPDIR=$SCRATCHDIR

%post
    # Install dependencies
    apt-get update && apt-get install apt-utils && apt-get install -y \
        python3.7 \
        python3-pip \

    # Install your data processing application
    pip install deepinterpolation \
